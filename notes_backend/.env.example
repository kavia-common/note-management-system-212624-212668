# Path to SQLite database file. If not set, defaults to ../notes_database/myapp.db relative to this backend.
NOTES_DB_PATH=/absolute/or/relative/path/to/myapp.db

# CORS origins (optional, not currently read in code but recommended for production)
# ALLOW_ORIGINS=https://your-frontend.example.com
